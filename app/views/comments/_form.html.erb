<%= form_for @article.comments.new , url: article_comments_path(@article, @article.comments.new) do |form| %>

  <% if @article.comments.new.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@article.comments.new.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @article.comments.new.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :commenter %><br>
    <%= form.text_field :commenter %>
  </p>
<!--  <p>-->
<!--    <%#= form.label :commenter_confirmation %><br>-->
    <%#= form.text_field :commenter_confirmation %>
<!--  </p>-->
  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>